<!DOCTYPE html>
<html>
  <head>
    <title>Breakthrough</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
   
  </head>
  <body>
    <%= button_to "Sign out", destroy_user_session_path, :method => :delete, class:"btn btn-link"%>
    <p class="list-unstyled"> Current_user: <%= current_user.email if user_signed_in? %></p>
    <p class="list-unstyled" class="notice"><%= notice %></p>
    <p class="list-unstyled" class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
